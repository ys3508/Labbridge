# LabBridge — Working Diary

Append-only. Agents read the last ~20 entries only; older entries are in JOURNEY-archive/.

A running log of how this project got built: the questions raised, what was wrong, and how we fixed it. Read top to bottom as a story, or skim the bold lines.

*Living document — updated as the work continues.*

## How we worked

A simple loop, repeated: **build a slice → test it like a real user → notice what's off → say it plainly → diagnose → fix → verify → commit.** Almost every good decision here started as a blunt observation ("this is awful," "I don't like that label," "why doesn't it warn me earlier?"). The product got sharp because the questions were honest, not because the first build was right.

---


## Phase 13 — Moments: a manager beside you, not a lesson

**Q (Sissi): "The workspace should feel private — 沉浸式学习, no distraction. Too many progress bars. And embed a learning toolbox (notes / screenshot / chatbot / 模拟软件)."**
Two features, one philosophy — subtract the chrome, add quiet help:
- **Focus mode, automatic.** Entering a task's moments collapses everything non-essential: the sidebar disappears, the header becomes one line (☰ Workspace · task title · 1/3), the four redundant progress indicators reduce to a hairline + tiny count, the dots hide, and the stage centers at reading width. Wrap or ☰ restores the full workspace — the folder is for *between* tasks. (She counted four progress indicators on one screen; she was right.)
- **The Toolbox** — a slim rail that survives focus mode, opening overlays so help never navigates away: **Notes** (per-task thinking space, separate from the Draft, persisted, exported with the project under `### Notes`), **Glossary** (every keyTerm in the plan, searchable — instant "what did that word mean?" with zero AI), **Snapshot** (her "screenshot," honest mechanics: captures the current moment's content into Notes with a 📌 reference — no browser permission circus), and **Coach** (the demo's sample coaching moved here from the Coach beat; in real mode the icon doesn't render until `/api/coach` exists — no dead buttons). 模拟软件 (the interactive worked-example table) parked on the ledger pending structured rows from the live model.
All verified live: auto-focus on entry, notes persist and ride the export, glossary search hits, snapshot appends, ☰ restores.
**Follow-up: "why so much empty space around the workspace?"** — Claude had capped the focused *container* at reading width instead of just the paragraphs (which already carry their own measure), leaving a white card floating inside a white card. Fixed: in focus the outer shell dissolves entirely — borderless slim header on the ground, zero padding ring, the moment card as the single surface at a wider column. The work owns the screen now. One process note: two of the day's python patch scripts died mid-run and silently discarded their in-memory edits — caught both times by post-write boolean checks, which are now the habit.

**Q (Sissi): "Can I insert YouTube tutorial videos — legally, without paying?"**
Yes — through the one sanctioned door: YouTube's official embeddable player (their ToS explicitly permits it; creators who object can disable it per-video). The don'ts stay dont's: no re-hosting, no ad-stripping, no transcribing videos into "our" teaching. Product-wise, videos became just another **verified resource kind**: YouTube's keyless oEmbed endpoint confirms a video exists and returns its true title/channel — verify-and-drop for video, at zero cost. Her first find ("What is Real-World Evidence?", AbbVie) was oEmbed-verified live and now plays **inside the moment** via the privacy-mode (nocookie) player — watch without leaving, exactly the 沉浸式 direction. Fixing the resource layer also settled an older debt she'd exposed: the demo's two placeholder resources (invented catalog IDs wearing real ✓ badges) were replaced with live-verified OpenAlex papers (Funk 2014 on claims misclassification; the RECORD reporting statement). No resource in the demo is fake anymore. Live-pipeline video search goes on the API-day ledger.

**Q (Sissi): "When we put papers, can we cite the most important information so users get it right away?"**
Yes — with one honest rule: **cite from fetched text, never from memory** (a model "summarizing" a paper it hasn't read is hallucination with a citation attached). Resources gained a `keyPoint`: 1-2 sentences quoted VERBATIM from the paper's abstract (OpenAlex serves abstracts free), attributed "— from the abstract," under the use note. The live fetch demonstrated the rule immediately: the RECORD statement has an abstract → it got a real quoted key point; Funk 2014 has no abstract in OpenAlex → it honestly got none. At API day the select stage fetches abstracts and Haiku picks the task-relevant sentences verbatim (ledger). Verify-and-drop now extends to citations.

**Q (Sissi): "What actually costs API? If I scroll around a generated plan, does that bill?" — then she topped up.**
Mapped every action to its cost (scrolling/working in a plan = free forever; building = ~$0.35-0.50; each settled resume edit = a small Haiku call) — and the audit found a real spend trap: **the plan lived only in memory, so every page refresh re-billed a full Opus generation.** Shipped the armor before her first paid run: a **generation cache** — plan + resources + self-check stored in localStorage keyed by an input hash. Same inputs → the paid plan loads instantly, free; changed inputs or Retry → fresh generation, cache overwritten; demo mode bypasses (personas share an empty form and would collide). Also flipped `WEB_AUGMENT` off for validation economy. Pay once per plan; own it.
**Follow-up: "can you make it save this time's plan?"** — the cache was already live, but her phrasing exposed the missing half: the cache keeps a plan *in the browser*; it doesn't give you a *file*. Added **"Download this plan (.md)"** to the Why-this-plan drawer — the full generated plan (mission, strengths/gaps, every task's context/concept/traps/example/check/assignment/ask-your-team/self-check, the readiness arc) as portable markdown. A paid plan is now savable, shareable, and reviewable outside the app — exactly what validation day needs.

**Validation run #1 attempt: "Plan generation failed."**
Her first paid run hit a real API wall we'd built ourselves into: `400 — the compiled grammar is too large`. Every field added this week made the structured-output schema so detailed that Anthropic's grammar compiler refused it (and probes confirmed the constraints: every object must be strict + fully enumerated, so you can't just loosen the leaves). The escape: **generation now uses a FLAT schema** (strings and string-arrays are grammar-cheap) and the route adapts it back to the rich nested shape the whole app consumes — `"term — meaning"`, `"label | query"`, `"Stage | timing | goal"` string formats, split server-side. Penny-sized Haiku probes verified the fix before asking her to spend again; her failed 400s billed nothing. Bonus intel from the same run: **live resume extraction worked well** — her annotation asked to check keyword accuracy, and the extracted field/sector/skills matched the fixture resume cleanly.

**VALIDATION RUN #1 — the real model, under the full contract: PASS.**
Third attempt (after the grammar fix) generated the plan this whole week aimed at. Concepts ~200 words that actually teach, senior-grade traps, ask-your-team questions better than the spec imagined, flagged conventions everywhere a one-shot would have asserted facts, the horizon compressed to her real 4-week deadline instead of a canned 30-60-90, a coherent five-task project ending at her actual ticket — and every honesty gate intact. The "is the content too little / will it get better with API" question that she asked three times is now answered with evidence: **live output roughly doubled the mock's teaching density on the first run, zero tuning.** Open nits logged (duplicate capstone label, lifecycle/OMOP still thin) in `revise/validation-notes.md` #4.

**Q (Sissi): "Now I'll review the paid plan page by page — don't change anything until I finish. No action; brainstorm with me and make notes." (in progress)**
The most disciplined stretch of the project: a two-reviewer read (Sissi structural, Codex technical) of the live plan, one beat at a time, with every finding numbered into a ledger instead of fixed on the spot. Task 1 fully reviewed (8 beats), Task 2 through the Example beat — **55 items** in `revise/plan-review-notes.md`. The headline findings: the *content* largely passed (bridges and ask-your-team blocks are reliably senior-grade) but the *loop* failed the same way three times — the app asks the user to attest to work it gave them no way to do (tick profiling of tables that don't exist; self-check a draft one page before the draft box; celebrate "You closed a gap ✓" while the same screen prints "Draft still empty"). Root cause: no data and no review step. The wrap bug turned out to be a variable literally named `doneAsIf` — completion fabricated by navigation, our own earned-state rule violated ten lines from a comment bragging about removing unearned-state relics. Also confirmed: the page-2 and page-3 templates have slot-level defects that reproduce identically across modules, and the generator commits about one confident simplification per module that a senior person would have to un-teach (lab claims ≠ lab values; first fill ≠ initiation without a washout) — which begat the idea of an "un-teach scan" dimension for the existing plan checker. Her must-do pair before any real user: ship the synthetic CSVs, and gate the checkmark on the draft being non-empty. Consolidation into a prioritized spec fires the moment she calls the review complete.

**Q (Sissi): "You've got a list based on my review right? do you want to do it now?" — go-ahead on phases 1+2.**
The loop got closed. In one pass, approved beat by beat: (1) **The wrap gate** — `doneAsIf` is gone; "You closed a gap ✓" renders only when the draft exists AND all criteria are ticked; an empty draft gets an honest amber "This task isn't done yet — go back and write it, or move on and accept the gap"; `markDone` (sidebar ✓, briefing chips, files-built counter) now requires a non-empty draft, so skipping stays legal but is never recorded as a win. (2) **The reshuffle** — her grammar revision: Try → Draft → Coach, so the self-check and review react to a draft that exists; Try became job-AND-bar (steps + done-when + criteria + watch-fors up front); the □ glyphs that looked like dead checkboxes became numbered steps. (3) **`/api/coach` is real** — the "AI review is coming" placeholder is deleted; a Haiku call grades the draft against the task's own criteria and red flags (met/thin/missing per criterion with quoted evidence, red-flag hits only when the mistake actually appears, 2-3 concrete next edits), button-triggered only, last review cached per task; the demo serves a canned state-honest sample so `?mock=1` stays zero-API. (4) **Tasks chain in state, not just narrative** — task N+1's Brief warns when the previous file is empty and QUOTES the user's own first real sentence when it isn't. (5) **The template button** — the empty-box killer: one click prefills the memo shape code-built from the task's own deliverable and steps. Verified end-to-end in the demo (empty path → honest wrap; filled path → review → earned gap-close → carry-forward quote) plus one live ~1¢ smoke test of the endpoint, which caught a red-flag over-trigger and got the prompt tightened. Bonus: a latent one-word bug — `Module` never destructured `focus`, so JSX silently passed `window.focus` (always truthy) and the moment dots were hidden even outside focus mode. Still queued: the prompt batch behind one ~$0.40 verification run, the un-teach scan, and the synthetic extract.

**Q (Sissi): "I already closed yesterday's web page." — which exposed a spend trap in the cache design.**
The gen-cache key hashes the full payload *including the AI-extracted skill chips* — and extraction can word itself differently between sessions. So the same person re-pasting the same Persona-1 materials could miss their exact key and silently re-bill a full Opus generation. The plan itself was never at risk (localStorage survives closed tabs); the *route* to it was. Fix: a **spend gate** — on a cache miss, if any saved plan exists in the browser, PlanView stops before calling the API and offers a choice: "Load saved plan — free" (dated, with the plan's own hook line) or "Generate a new plan (paid)". Verified live: seeded a saved plan, walked the real form with non-matching inputs, the gate appeared instead of a paid call, and the free load restored the plan *with* all prior workspace progress riding along. No accidental $0.40 ever again.

**The full-plan review ends at exactly 101 items — and the batch lands in one write session.**
The review reached 100% coverage (briefing, 5 tasks × 8 beats, readiness project, all on live output) and produced a clean verdict from both reviewers. Codex: teaching better than most paid RWE training, integrity architecture holding, one substantive gap — "the teaching is done; the only thing left in this product is the data." Sissi (item #101, and the sharper cut): the plan sells job-world time for a workspace-time product — "I don't even feel like it needs 90 days, only 3 hours is enough." With her go-ahead, everything shipped at once while she was out:
- **Door C — givens become real.** The review's longest-running wound (eight phantom filenames across five tasks) closed not with a synthetic dataset but with `/api/materials`: on demand, the model writes SMALL internally-consistent synthetic stand-ins for each task's givens (a 20-30-row extract, a filled dictionary), seeded with the module's own traps so they're discovered, not asserted — labeled synthetic, user-triggered, cached, field-agnostic because they're generated per plan. Live smoke test produced a 28-row extract whose dictionary even taught the billing-vs-service-date nuance unprompted. The plan's own Observe phase ("read a completed spec, reproduce the counts") had described exactly this fix (#100).
- **The two-clocks fix (#101).** Timeboxes now mean workspace minutes (30-60/task, the road sums to hours); the horizon is explicitly job-ramp context. Plus ~25 prompt-contract rules from the review: dependency order as a hard rule, explicit cross-task callbacks, coherence (criteria cover every step, checks test the concept), the check quality bar (scenario stems, smart distractors, length-matched — built from the one check that worked), matched criterion↔red-flag pairs, name-the-instrument/limit/shape, qualifiers ride with claims, ask-your-team never asks what the plan teaches, cold-start guards, "task" not "module."
- **Code batch:** type-aware draft templates (spec/table/memo — the placeholder bug that hit all five tasks), all drafts honestly .md, coach reviews now hunt the concept's own traps, prediction-framed self-checks, watch-fors live only on Try, final-task wrap gets capstone phrasing (the "next task builds on this file" string on task 5-of-5 was the old disease caught in the new fix), "project is assembled" only when it is, and the #64 in-flight lock — a reload during generation now routes to the spend gate instead of silently re-billing (the leak had plausibly cost two generations).
- **The un-teach scan (#55)** joined the background checker: a senior-practitioner pass over the taught content hunting the "fine for a lesson, dangerous on the job" class that the review caught once per module (lab claims ≠ lab values; first fill ≠ initiation; metformin ≠ diagnosis confirmation).
All verified in the demo (materials generate with seeded traps, spec-shaped template on the spec task, all three wrap states honest, coach flow intact) plus one live materials call. What remains is hers: one paid Retry on Persona-1 to verify the prompt batch against the 9-point checklist in `revise/review-consolidation.md`.

**The feedback batch ships: the product now talks back in the right voice.**
Six pieces, one session, all demo-verified plus two live tests:
- **Purpose-aware coach tone** — the sharpest idea from the feedback list, now real: verdicts for starting-role and interview (which now also judges "would this answer land in the room, and does it survive the push?"), a REFLECTIVE mentor for career-move, nothing for curious. Live test of the reflective branch was the day's best output: it mirrored the learner's own words ("precision was satisfying, you lost track of time — that's embodied data, not interpretation"), separated evidence from imagination, and asked instead of correcting ("is 'scared of starting over' a deal-breaker or a temporary ego adjustment?"). Pass/fail on "does this fit me?" would have broken the lenses; this keeps them.
- **The draft linter** — a zero-cost live fact spell-checker: cite a file, code, or patient ID that isn't in your materials/canon and an amber strip names it within a second ("Cited, but not in your materials: bogus_file.csv · E99.9 · M999" — and it correctly passed the two real citations in the same test). The phantom-artifact class, now caught at the keystroke.
- **"Discuss this review"** — a coach verdict hands itself to the assistant panel as an auto-sent seed; the bounded conversation the feedback list wanted, for free off last session's build.
- **Canon-to-coach** — the grader now receives the world's fixed facts alongside the materials.
- **👍/👎 on every beat** — the OTHER direction of "feedback system": users→product. One tap logs which beats work; a 👎 takes an optional "what's off?" note (stored plan-scoped, capped, backend when accounts land). Ten users from now, this replaces a 101-item manual review.
- **Tap-a-term** — key terms are chips; tap for the plain meaning inline. The glossary, one tap from the prose it serves.

**Q (Sissi): "Next: a feedback system and an interactive system — and check this Phase-A prompt draft, borrow what's valuable."**
Adjudicated both against the real codebase. The feedback list's #1 priority ("make Coach content-aware") was already shipped and acceptance-tested — the useful residue was four genuinely new pieces, now queued: purpose-aware feedback TONE (verdicts for role/interview, reflective mirroring for career-move, nothing for curious — pass/fail on "does this fit me?" would break the lenses), a zero-cost draft linter (cites-a-code-not-in-your-data, live), "discuss this review" (coach verdict → assistant panel, pre-seeded), and canon-to-coach. Plus the missed reading of "feedback system": users→product feedback (👍/👎 beat chips feeding regeneration and her telemetry).
The Phase-A prompt draft: mostly a clean mirror of decisions already shipped — but three borrows landed same-day: **timeEstimateMin as an integer** on every task (honest denominator for budget math and the tracker, replacing regex-parsing of prose timeboxes), **trims[] as structured output** (runway cuts now render on the roadmap: "Deferred to fit your runway — real gaps, honestly cut for time, not forgotten"), and the best one — **the entitySheet moves to PLAN generation** (the canon is defined at birth, so worked examples and practice materials finally derive from one world; the P001-vs-M001 seam closes architecturally). One firm rejection: the draft's unit-count clamp bands ([3,7], [4,12]) — that's budget÷unit-time with quotas, which quietly re-imports the always-5 disease; gaps set the count, period.

**Q (Sissi): "Why does every task have the same structure? It's boring, and the questions you ask are not valuable at all."**
Both correct, one root: the 8-beat grammar was validated for ONE task and repeated five times without ever being designed ACROSS tasks — and it treated the task-5 learner as the same person who started task 1. Three fixes, shipped:
- **Scaffolding fade**: task 1 teaches in full; middle tasks compress to what's NEW ("you know how claims think now"); the final task is near-pure production — empty fields make beats disappear, and that shrinkage IS the reward. The plan visibly takes the training wheels off.
- **Task archetypes** (new schema field, one string): learn_and_do / critique (find the flaws in a handed artifact — QC is half the job) / shadow_reproduce (reproduce a completed example — the real rite of passage) / plot_twist (the stakeholder changes the ask mid-task). No two adjacent tasks share a shape; a header chip announces it ("find the flaws", "expect a twist").
- **Question genres**: the always-MCQ ritual demoted — explain-back, predict, and spot-the-flaw are FREE-TEXT, typed and graded against the generated key via the coach endpoint (~1¢); MCQ survives once per plan at most (and as rapid-fire in interview mode). Recognition tests nothing; production does. Ledger #24, finally cashed.
Verified in demo: MCQ path intact on task 1, the free-text path live on task 3 (typed answer → "Close — but thin" → the key revealed). This was the last of the four same-shit complaints: same plan (fixed by grammars), same length (fixed by gaps), same task shape and same questions (fixed here).

**Q (Sissi): "Why is it always 5 tasks? Why does 'interview' change nothing? The whole frame is so fixed — why is this personalized??????"**
The most product-defining challenge of the project, and she was right: purpose changed the prose, timeline changed nothing, and the depth band's "4-6" was just "5" wearing a disguise. Her redesign directive, sharpened over three exchanges: **stops = the actual gaps between THIS background and THIS goal, fitted to the actual time — and purpose picks the grammar, not the adjectives.** Then a brainstorm she greenlit whole ("all of it I love it"), and one big build:
- **Gap-derived count.** No bands, no quotas, an explicit anti-middle rule: a near-expert gets a 2-stop road ("done Thursday"), a beginner gets 8 — the road's LENGTH is now evidence the background was read. Time budget fits the gaps both ways: too many → keep the job-critical ones and defer the rest BY NAME; too few → never pad, finishing early is a feature. The roadmap now says the quiet part: "— so this road has no stops for what you already do. Your background just made it shorter."
- **Four purpose grammars, one schema** (the grammar-limit wall forbids four schemas — fields REINTERPRET instead): starting_role stays the job simulation (Day-One Pack framing, week-zero/week-one split); interview becomes DRILL (units = concepts they'll grill you on, model answers, pushback drills, resume-anchored behavioral answers, defend-the-gap, cram ordering, an answer bank, a mock-interview capstone); career_move becomes ORIENT-THEN-DECIDE (evidence ledger, enjoyment-as-data, a counterfactual stop, decision-brief capstone); curious becomes A TASTE (≤3 stops, one-surprising-thing contract, myth-vs-reality, no homework beats, ends at a DOOR). Render: beat identities per purpose (Question/Answer/Rehearse/Bank it…), curious drops coach/draft machinery, the wrap becomes the door.
- **The time tracker** — active minutes only (tab visible, task open, input in the last 2 min), "⏱ 23m worked · ~3h planned · on pace" in both headers. The timeline makes a promise; the tracker shows it being kept.
- **The split-pane assistant** — her "read on the left, ask on the right": a docked panel off the Toolbox rail (✳), every message auto-carrying the current beat, task, materials, draft, and world canon; three quick actions (Explain simpler / Another example / Quiz me); the plan's honesty contract inherited verbatim. Live smoke test: asked why a code isn't proof of disease — it answered from the page AND cited M002's actual asthma row from the practice materials.
- **Language rules**: home-vocabulary derivation (every new concept lands in their world's words once) and output-language honoring ("队列定义 (cohort definition)" — prose in their language, field terms kept in English with a gloss).
The rest of the brainstorm (four visual roadmap identities, mock-interview runner, post-interview loop, exports, form-free curious, purpose-ladder handoff) is specced in `revise/purpose-grammars-spec.md` and dated in TASKS.md. Verification day: one interview-purpose generation is the before/after benchmark.

**The verification run passed, the review went to 100% twice, and batch 2 made the world hold together.**
Her paid Persona-1 run confirmed the prompt batch working on live output: minutes-not-days timeboxes, codes-before-cohort ordering, the washout taught as the centerpiece with P001 carried correctly across tasks, scenario-style checks (Task 5's "bigger sample → then the causal claim will hold" distractor punishes the power-vs-identification confusion — graduate-seminar test design), and bridges honest in BOTH directions (role-only run: universal register; persona run: her actual health-department resume, nearly verbatim). Codex ran the plan as the persona and walked out saying the exact thing the product sells: "I did a real piece of the job and it wasn't over my head." The coach passed the fluent-but-wrong acceptance test 4/4 — it quoted the M002 albuterol misclassification back verbatim.
What the run exposed was one level down: **the materials layer had no shared world.** Drug A was a metformin product in task 2, gemcitabine in task 3, and three different dose ladders across the plan; task 5's generated profile declared the data source "EHR" in a plan that taught claims for four tasks; generated code references twice paired real ICD codes with wrong meanings. Sissi's own verdict cut deepest: the plan felt thin and boring, and her instinct — real videos, real papers, real guidance — turned out to be describing a feature that already existed but was buried in a collapsed drawer she never opened.
Batch 2 (her go: "Yes"): a plan-scoped **world canon** — the first materials generation defines the entities (drug identity, code sets, data-source type, ID scheme) and every later task receives it as law (live-verified: brand, NDC family, strengths, IDs all held); a **reference-docs-must-be-correct** rule plus an Opus fact-check pass on coding references (live-verified: all twelve E11 subcodes correct after the pass); materials **auto-generate** on arrival (even Codex misread the pre-click state four times — every user would), render CSVs as **real tables**, carry download links, and guard Regenerate behind a confirm when a draft exists; the draft template button now prefers **the plan's own generated template**; the Model beat got the **compact-model hoist box** (5/5 tasks had the wall) and a **"From the field" verified anchor** surfaced where the concept is taught — plus WEB_AUGMENT back on and a select-stage prefer-the-anchor rule, which together answer "thin and boring" with real, catalog-verified field artifacts; the coach now receives the actual materials so empirical claims are checkable; "(from task N)" givens get annotated with the real file and its real state. The in-browser data workspace (DuckDB-WASM — her "work directly on the data" idea, and the concrete form of the long-parked 模拟软件) is specced in `revise/data-workspace-spec.md` for its own session, and TASKS.md's honest ledger got the full talked-never-did refresh.

**Q (Sissi + Codex, round 2): the mock-mode confusion, then the real cold-start run.**
Her "role-only" screenshots turned out to be the DEMO being served against real inputs — `lb_mock=1` had persisted from old demo links, so the sample plan's Persona-1 content (epi background, Meridian) looked like a wild hallucination from a role-only input. It wasn't; no model had run. Then she ran the real thing: a genuine role-only generation — the cold-start test, answered early — and the live generator **passed the hard part**: "✓ Starting fresh," no invented background, no invented employer, horizon labeled "assumed default." Codex's remaining catches were real and went on the ledger: the conditional-wearing-a-checkmark ("✓ *If* you have ANY spreadsheet experience"), a genuine dependency-order bug (cohort definition sequenced before the codes it's made of), beginner depth calibration (confounding adjustment at "1–2 days" for a fresh starter), and timeboxes still calibrated to an epi persona. One hardcoded cheque got fixed on the spot: the roadmap's "Nothing on this road assumes anything you don't already have" — a promise the app printed regardless of content — scoped down to the defensible "each stop is built from the one before it."
The day also bought two pieces of spend armor the hard way: (1) Claude's own test fetch with an empty `{}` body sailed past the plan route's validation and billed ~two real generations — `/api/plan` now refuses effectively-empty payloads before touching the API (0.18s, $0, tested); (2) with two real plans now saved in her browser, the restore gate became a **picker** — every saved plan listed with date, hook line, and task count, each loadable free, the paid path explicit at the bottom. Also logged: refreshing mid-generation double-bills (the cache only saves on completion) — an in-flight guard is on the ledger.

**Q (Sissi): "Interview should be the highlight — can the two-question diagnostic build trust?"**
Codex implemented the next interview-stage pass in two layers. First, the diagnostic got the trust guardrails: a short "map from you, not a template" frame, explicit Q2 partial skip ("the map loses its substance read"), a Q2 receipt guard that falls back to an honest standard role question instead of pretending a weak citation came from the posting, and softer gentle-tone contract copy. Then voice shipped as a reusable `VoiceInput`: browser Web Speech when available, typed fallback always, transcript confirmation before grading, no-audio privacy copy, delivery metrics (duration, WPM, fillers, pauses, false starts), freeze nudges, and Q2 skip from the dead-air lifeline.

The first real browser test exposed the right kind of embarrassment: Codex's in-app browser reported enough speech support to show "Start speaking," then failed at runtime and left the user in the voice panel. Fixed immediately: a failed mic attempt now disables Voice for that browser session, switches to Type, and says to open Chrome/Edge for mic testing or type here. Final trust-copy pass: no hard timer on the two questions; Q1 uses tone-aware guidance ("most good answers land around a minute — don't watch the clock" vs. gentle "walk me through it naturally"), voice/type copy is detected instead of conditional, and confirmed transcripts + final coaching signals are saved browser-locally for the plan with a plain promise: no audio stored. Earlier take history remains session-only unless/until a future coaching disclosure makes persistence kind instead of surveillant. Verified by production build, fixture check, and clean dev-server restart after respecting the `.next` cache rule.

**Q (Sissi): "Move to the next stage — build Triage v1 and the output contract."**
The diagnostic now pays off before generation. Codex added a post-diagnostic **Triage** stage: three rows (substance, delivery, the thing the user named), a confident "biggest lever" call, the humility seam ("from two answers, so first read"), and the correction door as a real reorder control. Ranking is pure code in `lib/triage.js`: `weakness × round relevance`, with round weights for recruiter/technical/hiring-manager/case/panel/not-sure, and the named challenge always included so the tool cannot rank away the user's dread. Runway now forks volume inside the triage copy (tomorrow = one lever, few days = top 2-3, weeks = full map) while the tone dial keeps warmth orthogonal — fast is not cold.

The output contract ships without a plan schema change: `TriageView` emits mutable `priority[]` (`dimension`, `rank`, `rationale`, `source=computed|user-override`) into the same steering-notes block as diagnostic results, persists it in localStorage as the adaptive seam for later drills/mock re-ranking, and the plan prompt gained one narrow rule to honor `TRIAGE PRIORITY`, especially user overrides. `revise/triage-spec.md` is now the durable design record; `TASKS.md` marks v1 built and leaves the Q2-relevance live gate open before calling the diagnostic fully shipped.

---

## Where it stands

Input (real extraction, honest job-link reading) → a **single-surface onboarding workspace**: a briefing doorway, manager-assigned tasks walked as Moments, drafts accumulating as real files (states, timestamps, downloads), earned-only progress, and a derived-horizon readiness arc. A zero-API **demo mode** shows the whole thing to anyone. It degrades to honesty, never to fiction — and the palette spec now literally encodes that rule.

## What's still ahead

- **Layout foundation → visual design** — the two specced passes that dress the finished skeleton (`revise/layout-foundation-spec.md`, `revise/visual-design-spec.md`).
- **When the API balance returns** — verify the model actually writes good `northStar` / `comprehensionCheck` / `closesGapIndex`; build `/api/coach` into the waiting sockets (the Coach panel and the parked manager's-reaction reward).
- **Cross-session memory** — accounts + a backend, so the workspace remembers you across devices. The stickiness play.
- **The skill graph** — code-verified prerequisite ordering.
- **Deploy** — rotate the key, set a spend cap, ship a link.

## Architecture at a glance

The flow, once you click **Build my plan**:

```
input  → /api/plan         structure only: strengths, gaps, task-modules, capstone
       → /api/candidates   propose canonical titles → verify against catalogs
       → /api/select       pick task-relevant resources, scoped to the exact section
       → /api/augment-web   thin modules only: one official doc/course via web search
       → /api/check         over-teaching + first-task-viability supervisors
```

**API routes** (all server-side; the API key never reaches the browser):
`analyze` (resume → skills+evidence, field, sector) · `classify` (artifact type) · `read-link` (fetch/parse a job URL or honestly fail; extract fields) · `focus` (review-screen phrase) · `plan` (course structure) · `candidates` / `select` / `augment-web` (retrieval-first resources) · `check` (plan self-check).

**Key files:**
- `app/page.js` — input flow + form state
- `components/` — `BackgroundSection`, `HeadedSection`, `GoalsSection`, `TimelineSection`, `ReviewScreen`, `PlanView`
- `lib/ai.js` — Anthropic client + model constants (`MODEL` = Haiku for cheap calls, `PLAN_MODEL` = Opus for plan/select)
- `lib/verify.js` — resource verification (Open Library, OpenAlex, web search)
- `components/MockGate.js` + `lib/mockResponses.js` — demo mode (zero API)
- `lib/moduleCheck.js` + `fixtures/` — static plan-quality checker + golden inputs
- `lib/constants.js` — pools, options, and the `WEB_AUGMENT` toggle

**Knobs:** `WEB_AUGMENT = false` (in `lib/constants.js`) for fast/cheap runs · swap `MODEL`/`PLAN_MODEL` to trade cost vs. quality · `ANTHROPIC_API_KEY` lives in `.env.local` (gitignored).

## The one-line takeaway

The product got good because it got tested honestly and the problems got named plainly. Keep doing that.
