export default function DrillCostNote() {
  return (
    <p className="text-xs leading-5 text-slate-500">
      A full drill loop costs about a cent; practice is bundled, not metered per take.
    </p>
  );
}
